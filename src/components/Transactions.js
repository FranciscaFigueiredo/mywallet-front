import { Records } from "../Styles/TransactionsStyle";

export default function Transactions() {
    return (
        <Records>
            <span>Não há registros de entrada ou saída</span>
        </Records>
    );
}